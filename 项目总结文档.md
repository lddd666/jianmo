# 渐墨（E-Ink Wallpaper Studio）项目总结文档

> 文档生成时间：2026-06-03  
> 项目仓库：https://github.com/lddd666/jianmo.git  
> 当前分支：deploy/render（HEAD: f608af9）

---

## 一、项目概述

**渐墨**是一款基于 Web 的墨水屏壁纸在线生成工具，能够将彩色图片转换为适合各类墨水屏设备显示的黑白灰度壁纸。项目采用经典的前后端一体架构（Flask + 原生 HTML/CSS/JS），支持 20+ 款主流墨水屏设备的分辨率预设，内置多种抖动算法和图像调整功能，并已部署至 Render.com 平台。

### 核心特性一览

| 特性 | 说明 |
|------|------|
| 多设备预设 | Kindle、Kobo、文石(Boox)、汉王、掌阅、MeeBook、PocketBook、iPhone、iPad 等 25 款设备 |
| 抖动算法 | Floyd-Steinberg、有序抖动 (Bayer) 4×4/8×8、Atkinson、固定阈值，共 5 种 |
| 灰度转换 | 亮度加权、平均值、去饱和度、红/绿/蓝单通道，共 6 种 |
| 图像调整 | 对比度、亮度、锐度、伽马校正、高斯模糊 |
| 智能裁剪 | 按设备比例锁定裁剪区域，滑块精确选择感兴趣区域 |
| 预设方案 | 6 个内置预设 + 用户自定义预设（localStorage 持久化） |
| 实时预览 | 所有调整即时生效，防抖自动预览 |
| 多格式导出 | PNG（无损）、BMP（无压缩）、JPEG |
| 部署支持 | Render.com 一键部署（Free Plan） |

---

## 二、技术架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────┐
│                   浏览器端（前端）                      │
│  ┌────────────┐  ┌────────────┐  ┌────────────────┐ │
│  │ index.html │  │ style.css  │  │    main.js     │ │
│  │  页面模板   │  │  样式文件   │  │  前端交互逻辑   │ │
│  │  (364行)    │  │  (891行)   │  │   (1276行)     │ │
│  └────────────┘  └────────────┘  └────────────────┘ │
│         │              │                │            │
│         └──────────────┼────────────────┘            │
│                        │ HTTP RESTful API            │
├────────────────────────┼────────────────────────────┤
│                   Flask 后端（app.py, 500行）          │
│  ┌───────────┐  ┌────────────┐  ┌────────────────┐  │
│  │  路由层    │→│  处理逻辑   │→│  图像处理引擎    │  │
│  │ (7个API)  │  │            │  │ (Pillow+NumPy) │  │
│  └───────────┘  └────────────┘  └────────────────┘  │
│                       │                              │
│              ┌────────┴────────┐                     │
│              │  image_store    │                     │
│              │ (内存字典≤50张)  │                     │
│              └─────────────────┘                     │
├──────────────────────────────────────────────────────┤
│              Gunicorn WSGI 服务器                      │
│         --bind 0.0.0.0:$PORT --timeout 120           │
├──────────────────────────────────────────────────────┤
│              Render.com 云平台（Free Plan）            │
│              Python 3.11 · 自动部署 · Umami 分析       │
└──────────────────────────────────────────────────────┘
```

### 2.2 技术栈详情

| 层级 | 技术 | 版本要求 | 说明 |
|------|------|----------|------|
| 后端框架 | Flask | ≥ 3.0.0 | Web 应用框架，Jinja2 模板引擎 |
| 图像处理 | Pillow (PIL) | ≥ 10.0.0 | 图像读写、变换、滤镜、格式转换 |
| 数值计算 | NumPy | ≥ 1.24.0 | 像素级数组运算，支撑抖动算法和灰度转换 |
| WSGI 服务器 | Gunicorn | ≥ 21.2.0 | 生产环境多进程 HTTP 服务器 |
| 前端 | HTML5 + CSS3 + JavaScript (ES6+) | — | 原生实现，零框架依赖 |
| 数据存储 | 内存字典 (Python dict) + localStorage (浏览器) | — | 后端无文件系统依赖，前端预设持久化 |
| 分析 | Umami | — | 隐私友好的网站访问统计（website-id: a4beedcd...） |
| 部署平台 | Render.com | Free Plan | GitHub 推送自动触发部署 |

---

## 三、项目文件结构

```
墨水屏壁纸生成/
├── .gitignore              # Git 忽略规则（Python/venv/IDE/OS/uploads）
├── LICENSE                 # MIT 开源许可证
├── README.md               # 项目文档（功能介绍、设备列表、安装指南、Windows 详细教程）
├── Procfile                # Render.com/Heroku 进程声明
├── render.yaml             # Render.com 部署配置（IaC 基础设施即代码）
├── requirements.txt        # Python 依赖清单（4个包）
├── app.py                  # 后端主文件（Flask 应用，500行）
├── templates/
│   └── index.html          # Jinja2 页面模板（364行）
├── static/
│   ├── css/
│   │   └── style.css       # 全局样式（891行）
│   └── js/
│       └── main.js         # 前端交互逻辑（1276行）
└── 新建 Text Document.txt  # 空文件（无实际用途）
```

### 文件职责矩阵

| 文件 | 行数 | 角色 | 核心职责 |
|------|------|------|----------|
| `app.py` | 500 | 后端核心 | Flask 路由、设备预设定义、图像处理流水线、7 个 API 端点 |
| `templates/index.html` | 364 | 页面结构 | 左右分栏布局、上传区、预览区、裁剪覆盖层、控制面板（8 个分区） |
| `static/css/style.css` | 891 | 视觉呈现 | 水墨风格设计语言、CSS 变量体系、响应式断点（1024px/640px）、动画 |
| `static/js/main.js` | 1276 | 前端逻辑 | 文件上传、预览生成、裁剪交互、预设管理、控件绑定、自动预览 |
| `Procfile` | 1 | 部署配置 | 声明 Gunicorn 启动命令 |
| `render.yaml` | 13 | 部署配置 | Render.com 服务定义（Python 3.11、Free Plan、环境变量） |
| `requirements.txt` | 4 | 依赖管理 | Python 包版本约束 |
| `README.md` | 132 | 项目文档 | 使用说明、Windows 完整安装教程、技术栈和项目结构说明 |

---

## 四、后端功能实现详解（app.py）

### 4.1 应用配置与初始化

```python
app = Flask(__name__)
app.secret_key = os.environ.get('SECRET_KEY', os.urandom(24).hex())
app.config['MAX_CONTENT_LENGTH'] = 10 * 1024 * 1024  # 10MB 上传限制
image_store = {}  # 内存图片存储（UUID → {data, width, height, filename}）
```

- **Secret Key**：优先从环境变量 `SECRET_KEY` 读取（Render.com 自动生成），否则随机生成
- **上传限制**：10MB，超出返回 413 错误
- **存储策略**：使用 Python 字典在内存中存储图片数据，最多保留 50 张（LRU 淘汰），避免对文件系统的依赖（Render.com 免费实例文件系统为临时性的）

### 4.2 设备预设系统（EINK_PRESETS）

定义了 25 款设备的分辨率预设，按品牌分组：

| 品牌 | 设备数量 | 设备型号 |
|------|----------|----------|
| Kindle | 3 | Paperwhite (1236×1648), Oasis (1264×1680), Scribe (1860×2480) |
| Kobo | 3 | Clara HD (1072×1448), Libra 2 (1264×1680), Sage (1440×1920) |
| 文石 (Boox) | 7 | Poke 4/5, Page, Note Air 1/2, Tab Ultra/C |
| 汉王 (Hanvon) | 3 | N10, N10 Mini, N10 Max |
| 掌阅 (iReader) | 5 | Ocean 3, C6, C7, Smart 3, Smart 4 |
| 其他阅读器 | 2 | MeeBook P78, PocketBook InkPad 4 |
| 手机/平板 | 5 | iPhone SE/15/15 Pro Max, iPad Mini/Air |
| 自定义 | 1 | 用户自定义宽度×高度 |

每款设备存储 `name`、`width`、`height` 三个字段。选择"自定义尺寸"时由用户输入宽高。

### 4.3 API 接口设计（7 个路由）

| 方法 | 路径 | 功能 | 请求/响应 |
|------|------|------|-----------|
| GET | `/` | 首页 | 返回渲染后的 HTML 模板，注入 `EINK_PRESETS` |
| GET | `/api/presets` | 设备预设列表 | 返回 JSON 格式的全部设备预设 |
| POST | `/api/upload` | 上传图片 | 接收 `multipart/form-data`，返回 `{image_id, width, height, filename}` |
| POST | `/api/preview` | 生成预览 | 接收 `{image_id, settings}`，返回 PNG 图像流（最大 1200px） |
| POST | `/api/download` | 下载壁纸 | 接收 `{image_id, settings, format}`，返回附件下载 |
| GET | `/api/original/<image_id>` | 获取原图 | 返回原始上传图片的预览尺寸版本 |
| POST | `/api/cleanup` | 清理内存 | 接收 `{image_id}`，从内存中删除对应图片 |

### 4.4 图像处理流水线（process_image 函数）

图像处理严格按照以下顺序执行，每一步的输入是上一步的输出：

```
原始图片
  │
  ▼
① 模式转换（RGBA→RGB 白底合成、P→RGB 等）
  │
  ▼
② 旋转（0°/90°/180°/270°）+ 水平/垂直翻转
  │
  ▼
③ 区域裁剪（crop_box: left/top/right/bottom）
  │
  ▼
④ 亮度调整（ImageEnhance.Brightness, 0~3.0, 默认 1.0）
  │
  ▼
⑤ 对比度调整（ImageEnhance.Contrast, 0~3.0, 默认 1.0）
  │
  ▼
⑥ 锐度调整（ImageEnhance.Sharpness, 0~5.0, 默认 1.0）
  │
  ▼
⑦ 灰度转换（6 种算法，见下表）
  │
  ▼
⑧ 伽马校正（gamma, 0.1~5.0, 默认 1.0）
  │
  ▼
⑨ 高斯模糊（blur radius, 0~10, 默认 0）
  │
  ▼
⑩ 缩放至目标尺寸（3 种模式，见下表）
  │
  ▼
⑪ 抖动算法（5 种算法 + 无抖动，见下表）
  │
  ▼
⑫ 颜色反转（可选）
  │
  ▼
输出图片
```

#### 灰度转换算法

| 方法 | 标识 | 算法说明 |
|------|------|----------|
| 亮度加权 | `luminosity` | `PIL.ImageOps.grayscale()` — 标准亮度公式 `0.299R + 0.587G + 0.114B` |
| 平均值 | `average` | `(R + G + B) / 3` |
| 去饱和度 | `desaturation` | `(max(R,G,B) + min(R,G,B)) / 2` |
| 红色通道 | `red_channel` | 仅取 R 通道 |
| 绿色通道 | `green_channel` | 仅取 G 通道 |
| 蓝色通道 | `blue_channel` | 仅取 B 通道 |

#### 缩放模式

| 模式 | 标识 | 行为 |
|------|------|------|
| 拉伸 | `stretch` | 强制拉伸至目标尺寸（可能变形） |
| 适应 | `fit` | 保持比例缩放，确保完整显示（可能有留白） |
| 填充 | `fill` | 保持比例缩放后居中裁剪至精确尺寸 |

#### 抖动算法

| 算法 | 标识 | 说明 |
|------|------|------|
| 无抖动 | `none` | 保留灰度值，不做二值化 |
| Floyd-Steinberg | `floyd_steinberg` | 误差扩散至右、下左、下、下右 4 个像素（权重 7/16, 3/16, 5/16, 1/16） |
| 有序抖动 4×4 | `ordered_4` | 使用 4×4 Bayer 矩阵 |
| 有序抖动 8×8 | `ordered_8` | 使用 8×8 Bayer 矩阵（由 4×4 递推生成） |
| Atkinson | `atkinson` | 误差扩散至 6 个像素，每像素扩散 1/8（总扩散 3/4，损失 1/4） |
| 固定阈值 | `threshold` | 可调阈值（0~255，默认 128），大于阈值为白，否则为黑 |

### 4.5 图片上传与存储机制

- 上传时生成 UUID 作为 `image_id`
- 图片以 PNG 格式序列化为字节流存入 `image_store` 字典
- 存储上限 50 张图片，超出后按插入顺序淘汰最早的条目
- 返回原始图片的宽高和文件名
- 支持格式：PNG、JPG/JPEG、BMP、GIF、WebP、TIFF

### 4.6 预览与下载

- **预览**：对处理后的图片进行二次缩放（最大 1200px），返回 PNG 格式图像流
- **下载**：返回完整分辨率的处理结果，支持 PNG/BMP/JPEG 三种输出格式
- **原图查看**：返回原始图片的预览尺寸版本（不经过任何处理）
- **清理**：客户端离开或重新上传时，调用 `/api/cleanup` 释放内存

---

## 五、前端功能实现详解

### 5.1 页面结构（index.html）

采用 Jinja2 模板引擎，页面结构为经典的左右分栏布局：

```
┌──────────────────────────────────────────────────┐
│                  Header（渐墨）                     │
│              E-ink Wallpaper Studio                │
├────────────────────────┬─────────────────────────┤
│    左侧：预览面板        │    右侧：控制面板          │
│  ┌──────────────────┐  │  ┌───────────────────┐  │
│  │   上传区域        │  │  │ ⚡ 操作按钮        │  │
│  │  (拖拽/点击上传)   │  │  │  生成预览/下载壁纸  │  │
│  └──────────────────┘  │  ├───────────────────┤  │
│  ┌──────────────────┐  │  │ 💾 预设方案        │  │
│  │   预览区域        │  │  │  加载/保存/删除     │  │
│  │  工具栏: 信息     │  │  ├───────────────────┤  │
│  │  tab: 效果图/原图 │  │  │ 📱 目标设备        │  │
│  │  操作: 重新上传   │  │  │  设备选择/自定义    │  │
│  │  图片+裁剪覆盖层  │  │  │  缩放模式          │  │
│  │  Loading层       │  │  ├───────────────────┤  │
│  └──────────────────┘  │  │ ✂️ 区域裁剪        │  │
│                        │  ├───────────────────┤  │
│                        │  │ 🎨 灰度转换        │  │
│                        │  ├───────────────────┤  │
│                        │  │ ⚙️ 图像调整        │  │
│                        │  ├───────────────────┤  │
│                        │  │ 🔳 抖动算法        │  │
│                        │  ├───────────────────┤  │
│                        │  │ 🔄 旋转翻转&其他   │  │
│                        │  ├───────────────────┤  │
│                        │  │ 💾 输出设置        │  │
│                        │  └───────────────────┘  │
├────────────────────────┴─────────────────────────┤
│                  Footer（渐墨标语）                 │
└──────────────────────────────────────────────────┘
```

### 5.2 CSS 样式系统（style.css, 891行）

#### 设计语言 — 水墨风格

采用 CSS 自定义属性（变量）体系，模拟中国传统水墨画的视觉效果：

```css
:root {
    --ink-dark: #1a1a2e;     /* 浓墨 */
    --ink-mid: #3a3a52;      /* 中墨 */
    --ink-light: #6a6a82;    /* 淡墨 */
    --paper: #f5f0e8;        /* 宣纸 */
    --paper-warm: #f0ead6;   /* 暖纸 */
    --accent: #4a6fa5;       /* 蓝色强调 */
    --success: #5b9a6f;      /* 绿色成功 */
    --danger: #c75c5c;       /* 红色警告 */
}
```

#### 关键样式特性

| 特性 | 实现方式 |
|------|----------|
| Header 渐变 | 从浓墨到宣纸的 4 段线性渐变 + 径向渐变装饰圆 |
| 控制面板分组 | 卡片式设计，左侧 3px 渐变色条区分不同功能区（每区独立配色） |
| 折叠面板 | CSS transition + `.collapsed` 类切换 `display: none` |
| 裁剪覆盖层 | 4 个半透明遮罩（上下左右）+ 虚线裁剪框 |
| 滑块样式 | 自定义 `::-webkit-slider-thumb` 和 `::-moz-range-thumb` |
| Toast 通知 | `fixed` 定位 + `toastIn`/`toastOut` 关键帧动画（3s 自动消失） |
| Loading 状态 | CSS `spinner` 旋转动画 + `backdrop-filter: blur(2px)` 毛玻璃 |
| 响应式 | 1024px 断点：左右分栏变上下堆叠；640px 断点：进一步紧凑化 |
| 自定义滚动条 | `::-webkit-scrollbar` 6px 宽度，毛笔灰配色 |

### 5.3 前端交互逻辑（main.js, 1276行）

#### 状态管理

使用全局 `state` 对象管理应用状态：

```javascript
const state = {
    imageId: null,        // 当前图片的 UUID
    originalWidth: 0,     // 原图宽度
    originalHeight: 0,    // 原图高度
    filename: '',         // 原始文件名
    cropEnabled: false,   // 裁剪是否启用
    cropBox: { x: 0, y: 0, w: 0, h: 0 },  // 裁剪区域
    isProcessing: false,  // 防止并发处理
};
```

另外维护 `transformState` 跟踪旋转和翻转状态。

#### 初始化流程（DOMContentLoaded → init()）

```
init()
  ├── initUpload()         # 拖拽/点击上传事件绑定
  ├── initCrop()           # 裁剪相关事件绑定
  ├── initControls()       # 控件事件绑定（设备选择、滑块、按钮）
  ├── initAutoPreview()    # 自动预览（change/input 事件 + 防抖）
  ├── initResizeHandler()  # 窗口大小变化时重绘裁剪覆盖层
  ├── initResetButtons()   # 各分区重置按钮
  ├── initTabs()           # 效果图/原图切换
  ├── initPresetSystem()   # 预设方案管理系统
  └── initAccordion()      # 折叠面板交互
```

#### 核心交互流程

1. **图片上传**：拖拽或选择文件 → 前端校验（类型/大小） → `POST /api/upload` → 切换到预览模式 → 自动生成预览
2. **预览生成**：收集所有控件设置 → `getSettings()` 构造 settings 对象 → `POST /api/preview` → 显示返回的 PNG 图像
3. **下载壁纸**：收集设置 → `POST /api/download` → 创建 Blob URL → 触发 `<a>` 标签下载
4. **自动预览**：滑块 `input` 事件 500ms 防抖、`change` 事件 300ms 防抖 → 自动调用 `generatePreview()`
5. **原图查看**：切换到"原图"Tab → `GET /api/original/{image_id}` → 直接显示原图

#### 裁剪系统实现

- 根据目标设备的宽高比计算裁剪区域
- 裁剪框按比例锁定，不能自由拖拽
- 通过滑块（0-100）控制裁剪区域的水平/垂直位置
- 裁剪覆盖层实时更新（4 个遮罩 + 虚线框 + 尺寸标注）
- 考虑旋转后的有效宽高（`getEffectiveWidth/Height`）
- 裁剪坐标从显示像素反算到原图像素（`getDisplayScale`）

#### 预设方案系统

**内置预设（6 个，不可删除）：**

| 预设名 | 目标设备 | 抖动算法 | 特点 |
|--------|----------|----------|------|
| 📖 Kindle漫画 | Kindle Paperwhite | Floyd-Steinberg | 高对比度+高锐度，漫画风格 |
| 📚 电子书封面 | Kindle Paperwhite | 无抖动 | 高锐度，保留灰度细节 |
| 🎨 复古艺术风 | Kobo Libra 2 | Atkinson | 高伽马，复古风格 |
| 📱 手机壁纸 | iPhone 15 | 无抖动 | 适配手机屏幕 |
| 🖼️ 高对比度 | 自定义 1404×1872 | 固定阈值 (128) | 极高对比度+锐度 |
| 📄 文字扫描件 | 自定义 1404×1872 | 固定阈值 (150) | 超高对比度+锐度，适合文字 |

**用户自定义预设：**
- 存储在 `localStorage`（key: `inkwallpaper_presets`）
- 保存当前所有控件状态（20+ 个参数）
- 支持保存、加载、删除、覆盖确认
- 与内置预设分组显示

#### 其他前端功能

- **旋转/翻转**：支持左旋90°、右旋90°、180°旋转、水平翻转、垂直翻转，状态累积
- **Tab 切换**：效果图/原图双 Tab，切换时自动加载对应图片
- **重置功能**：每个控制分区独立重置按钮（↺），带旋转动画
- **Toast 通知**：成功（绿色）、错误（红色）、信息（深灰）三种类型
- **Loading 状态**：处理中显示旋转动画和毛玻璃遮罩

---

## 六、部署配置

### 6.1 Render.com 部署（render.yaml）

```yaml
services:
  - type: web
    name: ink-wallpaper-generator
    env: python
    plan: free                    # 免费计划
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: SECRET_KEY
        generateValue: true       # 自动生成随机密钥
      - key: PYTHON_VERSION
        value: 3.11.0
    autoDeploy: true              # GitHub 推送自动部署
```

### 6.2 Procfile

```
web: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
```

- `--timeout 120`：请求超时 120 秒（图像处理可能耗时较长）

### 6.3 部署注意事项

- Render.com 免费实例的文件系统是临时性的，每次重启后文件丢失 → 项目采用内存存储方案
- 免费实例 15 分钟无请求后会休眠，首次请求需等待冷启动
- 环境变量 `SECRET_KEY` 由平台自动生成，`PORT` 由平台注入

---

## 七、Git 版本控制历史

### 7.1 分支概览

| 分支 | 类型 | 最新提交 | 说明 |
|------|------|----------|------|
| `master` | 主分支 | `2193220` | 原始开发版本，文件系统存储 |
| `deploy/render` | 部署分支（HEAD） | `f608af9` | 适配 Render.com 部署的版本，内存存储 + 环境变量配置 |
| `origin/master` | 远程主分支 | `2193220` | 与本地 master 同步 |
| `origin/deploy/render` | 远程部署分支 | `f608af9` | 与本地 deploy/render 同步 |

**当前工作分支**：`deploy/render`（活跃开发分支）

### 7.2 分支关系

```
d4cd323 ─→ e70339b ─→ f4a494a ─→ 2193220 (master)
                                    │
                                    ├──→ d6df034 ─→ f608af9 (deploy/render, HEAD)
```

`deploy/render` 分支从 `master` 的 `2193220` 提交分出，增加了 2 个部署相关的提交。

### 7.3 分支差异（master → deploy/render）

```
Procfile             |  1 +      # 新增：Gunicorn 进程声明
app.py               | 67 ±      # 修改：内存存储、环境变量配置、端口绑定
render.yaml          | 13 +      # 新增：Render.com 部署配置
templates/index.html |  3 ±      # 修改：添加 Umami 分析脚本
─────────────────────────────
合计：4 个文件，+51 行，-33 行
```

主要差异：
- **app.py**：将文件系统存储改为内存字典存储（`image_store`）、Secret Key 从环境变量读取、PORT 从环境变量读取、添加 `cleanup` API
- **新增 Procfile 和 render.yaml**：Render.com 部署所需
- **index.html**：在 `<head>` 中添加 Umami 统计脚本标签

### 7.4 完整提交记录

| # | 哈希 (短) | 日期 | 分支位置 | 提交信息 | 说明 |
|---|-----------|------|----------|----------|------|
| 1 | `d4cd323` | 2026-05-19 14:58 | (base) | `feat: initial release of JianMo (渐墨) - E-Ink Wallpaper Generator` | 项目首次发布，包含完整的墨水屏壁纸生成功能 |
| 2 | `e70339b` | 2026-05-19 15:05 | | `docs: fix clone URL in README` | 修复 README 中的 Git 克隆地址 |
| 3 | `f4a494a` | 2026-05-19 15:22 | | `docs: add Windows setup guide in README` | 添加 Windows 用户详细安装教程（Python/Git 安装、项目获取、运行步骤） |
| 4 | `2193220` | 2026-05-19 21:49 | **master** | `feat: 渐墨UI美化、预设方案保存/加载、旋转翻转、抖动算法优化` | 重大更新：UI 水墨风格重设计、预设系统（内置+用户自定义）、旋转翻转功能、折叠面板、裁剪比例锁定、多种灰度算法、Toast 通知、响应式适配 |
| 5 | `d6df034` | 2026-05-20 10:02 | | `feat: adapt for Render.com deployment - in-memory image storage, env-based config, Procfile, render.yaml` | 适配 Render.com 部署：文件系统存储→内存存储、环境变量配置、新增部署文件 |
| 6 | `f608af9` | 2026-05-20 11:18 | **deploy/render** (HEAD) | `feat: add Umami analytics tracking code` | 添加 Umami 网站访问分析追踪代码 |

### 7.5 提交时间线

```
2026-05-19
  14:58  [d4cd323] 项目首次发布
  15:05  [e70339b] 修复 README 克隆地址
  15:22  [f4a494a] 添加 Windows 安装指南
  21:49  [2193220] UI美化+预设系统+旋转翻转+抖动优化  ← master 分支
         │
         └──→ 分叉创建 deploy/render 分支
2026-05-20
  10:02  [d6df034] 适配 Render.com 部署
  11:18  [f608af9] 添加 Umami 分析              ← deploy/render 分支 (HEAD)
```

---

## 八、开发迭代总结

### 第一阶段：基础功能实现（2026-05-19 下午，提交 #1~#3）

- 完成项目骨架搭建（Flask + 原生前端）
- 实现基本的图片上传、处理、预览、下载流程
- 内置多款墨水屏设备预设
- 实现 Floyd-Steinberg 等抖动算法
- 文件系统存储方案
- README 文档编写

### 第二阶段：功能增强与 UI 美化（2026-05-19 晚间，提交 #4）

- **UI 全面重设计**：采用水墨风格设计语言，CSS 变量体系
- **预设方案系统**：6 个内置预设 + 用户自定义预设（localStorage）
- **旋转翻转功能**：支持 90°/180°/270° 旋转 + 水平/垂直翻转
- **裁剪系统升级**：按设备比例锁定、滑块控制、覆盖层实时更新
- **灰度转换扩展**：从 1 种扩展到 6 种
- **交互优化**：折叠面板、自动预览（防抖）、Toast 通知、Tab 切换
- **响应式适配**：1024px 和 640px 两个断点

### 第三阶段：部署上线（2026-05-20，提交 #5~##6）

- 适配 Render.com 平台：内存存储替代文件系统
- 环境变量化配置（SECRET_KEY、PORT）
- 新增 Procfile 和 render.yaml 部署文件
- 集成 Umami 网站分析
- 项目上线运行

---

## 九、已知特征与技术决策

1. **无数据库**：纯内存存储，服务重启后图片数据丢失。这是 Render.com 免费实例的限制所决定的设计决策
2. **无用户系统**：单用户、无认证，适合个人工具使用场景
3. **无前端框架**：纯原生 JavaScript，项目体量下保持了良好的可维护性
4. **单文件后端**：全部后端逻辑集中在 `app.py`（500 行），无蓝图、无模块拆分
5. **单文件前端**：全部前端逻辑集中在 `main.js`（1276 行），无模块化
6. **抖动算法在 CPU 上执行**：Floyd-Steinberg 和 Atkinson 为逐像素循环实现，大图可能较慢
7. **预览尺寸限制**：预览图最大 1200px，下载图为完整分辨率
8. **客户端预设持久化**：使用 localStorage，仅在当前浏览器有效